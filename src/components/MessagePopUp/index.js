import { Root } from "./MessagePopUpRoot";
import { Header } from "./MessagePopUpHeader";
import { Body } from "./MessagePopUpBody";

export const ModalCustom = {
  Root: Root,
  Header: Header,
  Body: Body,
};